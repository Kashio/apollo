#ifndef APOLLO_CORE_COMMON_H
#define APOLLO_CORE_COMMON_H

#include <cstddef>

namespace apollo
{
	typedef std::size_t id_type;
	typedef id_type entity;
	const std::size_t invalid_index = -1;
}

#endif // !APOLLO_CORE_COMMON_H
